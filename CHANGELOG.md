# 更新记录

用户每次开工时，skill 会顺手检查本地版本和仓库最新版本是否一致，落后就提醒一句。
手动更新：在 skill 目录里跑 `git pull`。

## v1.5 — 2026-09-11

- **条目结构改为「项目三段式、实习内嵌」。** 实跑中模型把经历库 1:1 誊成简历——24 条平均 48 字，长在过程叙述上（「横向对比同类产品后设计两档，先交付 SOP，再从 SOP 中识别可自动化环节」59 字零数字），HR 读不出能力。现在独立项目走：一句痛点背景 → 四字标签条目（做法 + 技术细节 + 直接产生的数字）→ 底部**成果**行放合力数字；实习经历不套成果行，用四字标签 + 内嵌 XYZ。
- **撤销 v1.0 的「禁止小标题」规则，换成带判据的版本。** 那条把用户原简历里「自动化提效：」这种写法当 AI 味禁掉了，是误判。新判据：标签后面必须跟技术细节或数字——「数据归因：订单 +31%、访客 +19%」是索引，「从数据中读出结论：我发现了规律」是自夸。标签 4-7 字、用能力名词，不许用「我的职责」当大标题。
- **成果分两层。** 动作直接产生的数字留在条目里，项目合力的数字放成果行，全抽走断因果链、全塞进某条是假因果。成果行数字撑不起一行就省略，不是固定格式。
- **写法规则新增**：技术细节要可追问（「3 个 Agent 替代一人一天」是空的，拆成一条一个工具一个动作）；写清楚替代了什么手工动作；字数花在技术细节和数字上不花在过程叙述上，判据是「删掉一句会不会少一个可核对的事实」；同时防矫枉过正，40 字以下要检查是不是把细节砍没了。每段条目数改为 2-5 条按料定。
- **包装规则明写为必做项。** 经历库是原子事实，简历不是誊写；每条都要问「放到目标行业的语境里叫什么」。
- 篇幅一节新增：两页时检查翻页点是否落在章节边界。
- `模板.md` 骨架同步：独立项目在前带成果行，实习在后；约定表加 `- 四字标签：内容` 与 `**成果**：……` 两行。

## v1.4 — 2026-09-08

- **字体子集化，仓库 74MB → 41MB，`fonts/` 44MB → 11MB。** 完整思源含四万多字形，一份简历实际只用一千出头，等于让每个用户为排一页简历下载四本《康熙字典》。现在只保留 GB2312 全部汉字 6763 个（等同通用规范汉字表一二级）加符号，共 7565 字。观感、字重、行距、OpenType 特性全部不变；**缺字不会变豆腐块**——浏览器字体回退逐字生效，子集里没有的字落到系统装的完整思源或雅黑/苹方。
- **子集字体改名 `TailorSansCN` / `TailorSerifCN`，这是许可要求不是审美选择。** Adobe 对思源保留字体名 'Source'，OFL 1.1 第 3 条禁止修改版沿用保留名，而子集化就是 OFL 定义的「修改」。文件名和字体内部名称表都已改，nameID 3 保留 `subset-of-SourceHan` 供溯源。原来 `fonts/README.md` 里「原样分发未经修改的字体文件」那句已不成立，一并改掉。
- `fonts/subset-charset.txt` 随仓库分发，`fonts/README.md` 补上子集范围、重新生成的完整命令和改名脚本——以后换字体或加字时不用重新推导。
- 同步 `md2html.py` 的字体栈与 `FONT_FACES`、`SKILL.md` 的字体说明、`README.md` 的排版一节。

## v1.3.1 — 2026-09-08

- **修复多张 JD 截图被并成一份简历。** 实跑中发两张不同方向的 JD（视频后期 / AIGC 视觉），只拿到一份 PDF。两个原因叠加：阶段 3 原有的「多张按顺序读完再拆」本意是处理同一个 JD 的多页截图，被套用到了不同岗位上；v1.3 新增的「同一方向合起来拆」又默认 JD 已经归好类，没写归类这一步本身。
- **新增「收到截图先归类」**：逐张读出公司名和岗位名，区分「同一 JD 的多页 / 同方向的不同 JD / 不同方向的 JD」三种情况，把归类结果摆给用户确认后再拆。不靠张数猜。
- **新增方向清单对账**：有方向没收到 JD 就停下来问，不并进别的方向也不默默跳过；收到归不进任何已定方向的 JD 同样要报。
- **合并改为需要用户点头**：默认一个方向一份，只有拆完发现能力点高度重合才提议合并，由用户拍板。用户发了两个方向却只拿到一份 PDF，他会以为工具漏了，不会以为工具帮他省事。
- 明写阶段 3 的 7 步流程**对每个方向各跑一遍**，跑完报份数和路径，让用户能核对。

## v1.3 — 2026-09-08

- **新增阶段 1.5：定向验证与方向提案。** 产品重心从「把简历写好」扩到「先弄清楚你能投哪儿」。读完简历先免费给粗提案（你还能投哪些方向、依据是哪几件事），用户看到价值之后再决定要不要花五分钟做验证——先给再问，不是先问再给。
- **定向验证用选项，不用开放题。** 五档固定选项（主导做的 / 参与一部分 / 课程作业没实用过 / 简历写了但没做过 / 记不清），把「承认简历有水分」从一次坦白降级成一次点击。选项措辞中性、字数拉平、可降级成纯文本编号列表，不绑定客户端能力。
- **追问靶子改为「越界动作」。** 常规动作（负责调价、上下架）撑不起新方向，越出本岗位常规的动作（用 Claude Code 写脚本自动化）才是可能性的来源。5 条问题按「排假」和「挖真」各半分配，只问会改变方向清单的。
- **方向提案必须带硬门槛判定。** HR 初筛 5-15 秒先筛硬条件，方向对但门槛不过等于白投。同时明确区分「你已经在瞄的」和「你可能没想到的」——把用户已知方向包装成发现，是这个功能最容易翻车的地方。
- **交付单位从「一份 JD」改成「一轮同方向的申请」。** 定了 N 个方向不等于出 N 份简历——先比较各方向 JD 的能力点重合度，重合就合成一份并如实说明，不为证明「分方向」正确而硬造区别。
- **修复命中统计漏项。** 原来只给汇总数字（直接命中 X、可迁移 Y、缺 Z），数字能凑对但能力点会中途消失。实跑中出现过：JD 的「为决策层提供商业建议」整点丢失，同时把「Python 或 SQL」这个或然条件的未满足分支算成了缺口，总数正好凑齐 8，从数字上看不出问题。现在改为逐点对账表——每点必须显式归档、或然条件满足一支即算命中、表格行数必须等于能力点数，对不上就重新核对而不是增删条目凑数。
- **写明三条使用路径**（完整 / 只要简历 / 只要排版），但**不在开头让用户三选一**——信息不足时用户一律选最省事的。只要排版那条走完要挂钩子，把最轻的入口变成引流口而不是死胡同。

## v1.2 — 2026-09-06

- **教育背景改到上半区。** 之前模板把它写死在最后，那是社招排法；应届生第一道筛的就是学校和专业，藏在末尾等于让 HR 自己找。现在上半区固定「个人信息 → 求职意向 → 摘要 → 教育背景」，下半区「实习 / 项目 / 技能」由 Agent 按 JD 判断顺序。
- **条目里要点出具体工具名**（Python、Excel 透视表、生意参谋）。工具名既是能力证据，也是筛选关键词。没工具的经历不硬凑。
- 写明为什么禁止「负责」开头，防止后续被网上流传的「先写你负责了什么」改回去。
- **篇幅交给用户选。** 拆完 JD、动笔之前问一句：压成一页，还是内容优先不限页数。以前是默认往一页压。压缩在选料阶段完成，不再先写满后删。

## v1.1 — 2026-09-06

- **深挖改成可选。** 读完简历立刻落一版经历库，然后让用户自己选：直接投，还是先深挖。以前必须先聊二三十分钟才有产出，赶时间的人直接流失。
- **新增「补经历库」流程（阶段 2.5）。** 以后多了实习、项目、奖项，回来说一句「补经历库」就能增量补充，只问新东西，不重问老问题。
- 开场白里说明经历库是长期资产，以及怎么回来补。
- 字体目录补上 SIL OFL 许可副本与来源说明（思源黑体 / 思源宋体 / JetBrains Mono 均为 OFL，不适用仓库的 MIT）。
- 修正 `模板.md` 里过时的「侧栏 / 主栏」分栏规则——实际版面是单栏，章节按 markdown 顺序排。
- 补上排版环节跑不了脚本时的降级说明（罕见情况，只在确实执行不了本地命令时用）。

## v1.0 — 2026-09-05

- 首个公开版本：纯事实经历库 + 按 JD 组装 + 版式 1:1 复刻 LapisCV + 自动导出 PDF。
